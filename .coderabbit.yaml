language: en-US
reviews:
  profile: chill
  high_level_summary: true
  poem: false
  review_status: true
  auto_review:
    enabled: true
    drafts: false
  path_instructions:
    - path: "packages/core/src/**"
      instructions: "This is the MCP server core. Check that new tools follow the existing pattern: definition in definitions.ts, method in tools/index.ts, case in server.ts, handler in http-server.ts."
    - path: "studio-plugin/src/**"
      instructions: "This is roblox-ts code compiled to Luau. Uses pcall for error handling, pairs() for iteration, and roblox-ts types. Check that new handlers are registered in Communication.ts routeMap and exported from their handler module."
